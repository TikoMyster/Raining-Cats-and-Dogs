<div class="container-fluid">
    {{#each hotels as |hotel|}}
    <div class="border my-2">
        <h3>{{hotel.name}}</h3>
        <p>Address: {{hotel.address}}</p>
        {{> hotel-detail}}
    </div>
    {{/each}}
</div>
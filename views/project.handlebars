<div class="text-center">
  <h2>{{My Pet's Name}}</h2>
  <p>{{{My Pet's age}}}</p>
  <p>{{About my Pet}}</p>
  <p>Created by {{Username}} on {{format_date date_created}}</p>
  <p>
    <button class="btn btn-primary">Find a place to stay!</button>
  </p>
</div>
